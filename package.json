{
  "name": "roam-caret",
  "version": "0.6.0",
  "private": true,
  "description": "Custom text caret for Roam Research: lite CSS overlay by default, canvas effects on demand.",
  "type": "module",
  "main": "extension.js",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "test": "node --test test/*.test.js",
    "verify:generated": "node scripts/verify-generated.mjs",
    "check": "npm run build && npm run scan:secrets && node --check extension.js && node --check engine.js && npm test && npm run verify:generated"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  },
  "license": "MIT"
}
